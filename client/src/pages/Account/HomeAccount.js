import React, { useEffect, useState } from 'react';

const HomeAccount = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        flexDirection: 'column',
        alignItems: 'center',
        minHeight: '30vh',
      }}
    >
      <h2>Wähle einen Bereich in der Übersicht aus!</h2>
    </div>
  );
};

export default HomeAccount;
